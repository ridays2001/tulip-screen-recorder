# Tulip Screen Recorder

A screen recorder built with Electronjs.

# How to start:

-   Have a recent version of Nodejs installed in your system.
-   Open terminal in project root folder and use this command:

```bash
npm install && npm start
```
